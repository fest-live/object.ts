[**@fest-lib/object API Documentation v0.1.3**](../README.md)

***

[@fest-lib/object API Documentation](../globals.md) / delayedOrInstantBehavior

# Function: delayedOrInstantBehavior()

> **delayedOrInstantBehavior**(`delay?`): (`cb`, `__namedParameters`, `__namedParameters`) => `void`

Defined in: core/Primitives.ts:281

Same as `delayedBehavior`, but invoke immediately when the delay gate is not needed.

## Parameters

### delay?

`number` = `100`

## Returns

(`cb`, `__namedParameters`, `__namedParameters`) => `void`
