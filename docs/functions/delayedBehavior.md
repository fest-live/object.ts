[**@fest-lib/object API Documentation v0.1.4**](../README.md)

***

[@fest-lib/object API Documentation](../globals.md) / delayedBehavior

# Function: delayedBehavior()

> **delayedBehavior**(`delay?`): (`cb`, `__namedParameters`, `__namedParameters`) => `void`

Defined in: core/Primitives.ts:276

Create a deferred trigger behavior that aborts cleanly when the subscription is disposed.

## Parameters

### delay?

`number` = `100`

## Returns

(`cb`, `__namedParameters`, `__namedParameters`) => `void`
