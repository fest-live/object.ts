[**@fest-lib/object v0.1.31**](../../../README.md)

***

[@fest-lib/object](../../../README.md) / [core/Resolved](../README.md) / resolved

# Function: resolved()

```ts
function resolved(target, mode?): Promise<any>;
```

Defined in: core/Resolved.ts:24

Snapshot a reactive target (or its raw source) with `all` / `allKeyed` / settled variants.

## Parameters

### target

`any`

### mode?

`ResolvedMode` = `"all"`

## Returns

`Promise`\<`any`\>
